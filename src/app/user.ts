export class User {
    // properties of the user class
    constructor(
        public name: String,
        public email: String,
        public phone: number,
        public topic: String,
        public timePreference: String,
        public susbscribe: boolean
        ) {}
    // this is the syntax of a typescript constructor 
}
